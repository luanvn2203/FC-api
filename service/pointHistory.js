const db = require('./db');
const helper = require('../helper');

async function savePointHistory(accountId, point, typeOfUse, usingDescription, dateOfUse) {
    try {
        const sql = "Insert into tbl_point_history()"
    } catch (error) {
        console.log(error)
    }
}


module.exports = {

}